📌 Telnet이란?
Telnet은 네트워크 장비(스위치, 라우터 등)에 원격으로 접속하기 위한 프로토콜입니다.
TCP 포트 23번을 사용하며, CLI(Command Line Interface)에 접속할 수 있게 해줍니다.
하지만 통신 내용이 평문(plain text)으로 전달되기 때문에, 암호나 명령어가 그대로 노출될 위험이 있습니다.
그래서 보안성이 낮아 실제 네트워크 운영 환경에서는 거의 사용하지 않고, 학습이나 테스트 환경에서만 쓰이는 경우가 많습니다.

